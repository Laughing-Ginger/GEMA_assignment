# Local dev port for the Express server (ignored on Vercel serverless).
PORT=5000

# Optional: set this to enable MongoDB persistence.
# If unset, enquiries are kept in-memory so the API still works.
MONGO_URI=

# Frontend origin (used for local reference / future CORS tightening).
FRONTEND_URL=http://localhost:5173
